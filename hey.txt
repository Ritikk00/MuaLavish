import React from "react";
import { FaWhatsapp, FaRegClock, FaCheck, FaSun, FaLeaf, FaGlassCheers, FaCamera, FaCrown } from "react-icons/fa";
import { motion } from "framer-motion";
import { services, siteInfo } from "../data/siteData";

// Icon and color map helper based on service id
const serviceStyles = {
  "haldi-makeup": {
    icon: FaSun,
    accentColor: "border-brand-rose/20 text-brand-rose bg-brand-blush/20",
    badgeColor: "bg-brand-rose text-white",
    glowEffect: "group-hover:shadow-brand-rose/10"
  },
  "mehndi-makeup": {
    icon: FaLeaf,
    accentColor: "border-brand-rose/20 text-brand-rose bg-brand-blush/20",
    badgeColor: "bg-brand-rose text-white",
    glowEffect: "group-hover:shadow-brand-rose/10"
  },
  "party-makeup": {
    icon: FaGlassCheers,
    accentColor: "border-brand-rose/20 text-brand-rose bg-brand-blush/20",
    badgeColor: "bg-brand-rose text-white",
    glowEffect: "group-hover:shadow-brand-rose/10"
  },
  "engagement-makeup": {
    icon: FaCrown,
    accentColor: "border-brand-rose/20 text-brand-rose bg-brand-blush/20",
    badgeColor: "bg-brand-rose text-white",
    glowEffect: "group-hover:shadow-brand-rose/10"
  },
  "hd-makeup": {
    icon: FaCamera,
    accentColor: "border-brand-rose/20 text-brand-rose bg-brand-blush/20",
    badgeColor: "bg-brand-rose text-white",
    glowEffect: "group-hover:shadow-brand-rose/10"
  },
  "reception-makeup": {
    icon: FaCrown,
    accentColor: "border-brand-rose/20 text-brand-rose bg-brand-blush/20",
    badgeColor: "bg-brand-rose text-white",
    glowEffect: "group-hover:shadow-brand-rose/10"
  }
};

export default function Services() {
  const containerVariants = {
    hidden: { opacity: 0 },
    visible: {
      opacity: 1,
      transition: {
        staggerChildren: 0.1
      }
    }
  };

  const cardVariants = {
    hidden: { opacity: 0, y: 30 },
    visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: "easeOut" } }
  };

  return (
    <section
  id="services"
  aria-label="Professional Makeup Services,artist in Delhi NCR"
  className="py-20 bg-brand-nude/20">
      <div className="max-w-7xl mx-auto px-4 md:px-8">
        
        {/* Section Heading */}
        <div className="text-center max-w-2xl mx-auto mb-16 space-y-4">
          <span className="text-brand-rose text-xs uppercase tracking-[0.25em] font-bold">
            Freelance Services
          </span>
          <h2 className="text-3xl md:text-4xl font-serif font-bold text-brand-charcoal">
            Signature Makeup & Styling Services
          </h2>
          <div className="w-16 h-0.5 bg-brand-rose mx-auto"></div>
          <p className="text-brand-gray text-sm md:text-base font-light">
            Custom-tailored, camera-ready makeovers delivered directly at your doorstep or venue across Delhi NCR.
          </p>
        </div>

        {/* Services Grid */}
        <motion.div
          variants={containerVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true, margin: "-100px" }}
          className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-7xl mx-auto"
        >
          {services.map((service) => {
            const styles = serviceStyles[service.id] || {
              icon: FaCrown,
              accentColor: "border-brand-blush/30 text-brand-charcoal bg-brand-nude/10",
              badgeColor: "bg-brand-rose/90 text-white",
              glowEffect: "group-hover:shadow-luxury-hover"
            };
            const IconComponent = styles.icon;

            // Customize WhatsApp message for specific service
            const msg = encodeURIComponent(`Hello MUA Lavish! I would like to book the "${service.title}" service. Please let me know the availability.`);
            const waUrl = `https://wa.me/${siteInfo.whatsappNumber.replace("+", "")}?text=${msg}`;

            return (
              <motion.div
                key={service.id}
                variants={cardVariants}
                className={`group bg-white rounded-3xl overflow-hidden border border-brand-blush/10 hover:border-brand-rose/30 shadow-luxury hover:shadow-xl transition-all duration-500 flex flex-col h-full ${styles.glowEffect} transform hover:-translate-y-1.5`}
              >
                {/* Service Image */}
                <div className="relative h-60 overflow-hidden">
                  <img
                    src={service.image}
                  alt={`${service.title} Makeup Service in Delhi NCR by MUA Lavish`}
                    className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                    loading="lazy"
                  />
                  {/* Dark transparent overlay */}
                  <div className="absolute inset-0 bg-gradient-to-t from-brand-charcoal/80 via-transparent to-transparent opacity-70 group-hover:opacity-50 transition-opacity duration-300"></div>
                  
                  {/* Category Type Badge */}
                  <div className={`absolute top-4 left-4 ${styles.badgeColor} text-[10px] uppercase tracking-wider font-bold px-3 py-1 rounded-full shadow-md`}>
                    {service.title.split(" ")[0]}
                  </div>

                  {/* Icon Emblem overlay */}
                  <div className="absolute bottom-4 right-4 w-10 h-10 rounded-full bg-white/95 backdrop-blur-sm text-brand-charcoal flex items-center justify-center shadow-md border border-brand-blush/20">
                    <IconComponent className="text-brand-rose" size={16} />
                  </div>
                </div>

                {/* Service Body */}
                <div className="p-6 flex flex-col flex-grow justify-between space-y-6">
                  <div className="space-y-4">
                    {/* Title */}
                    <div className="flex items-center gap-2">
                      <h3 className="text-lg font-serif font-bold text-brand-charcoal group-hover:text-brand-rose transition-colors duration-300">
                        {service.title}
                      </h3>
                    </div>
                    
                    {/* Description */}
                    <p className="text-brand-gray text-xs md:text-sm leading-relaxed font-light min-h-[50px]">
                      {service.description}
                    </p>

                    {/* Features list */}
                    <ul className="space-y-2 text-xs text-brand-charcoal/80 font-medium pt-2 border-t border-brand-nude">
                      {service.features.slice(0, 4).map((feat, idx) => (
                        <li key={idx} className="flex items-start gap-2">
                          <FaCheck size={10} className="text-brand-rose mt-0.5 flex-shrink-0" />
                          <span className="leading-tight">{feat}</span>
                        </li>
                      ))}
                    </ul>
                  </div>

                  {/* Actions footer */}
                  <div className="pt-4 border-t border-brand-nude flex flex-col sm:flex-row sm:items-center justify-between gap-3">
                    <div className="flex items-center gap-1.5 text-brand-gray text-xs">
                      <FaRegClock size={12} className="text-brand-rose" />
                      <span>{service.duration}</span>
                    </div>

                    <a
                      href={waUrl}
                      target="_blank"
                      rel="noopener noreferrer"
                      className="inline-flex items-center justify-center gap-1.5 bg-brand-charcoal text-white hover:bg-brand-rose text-xs uppercase tracking-wider font-bold px-4 py-2.5 rounded-full cursor-pointer transition-colors duration-300 shadow-sm"
                    >
                      <FaWhatsapp size={14} className="text-[#25D366]" />
                      <span>Inquire Now</span>
                    </a>
                  </div>
                </div>
              </motion.div>
            );
          })}
        </motion.div>
      </div>
    </section>
  );
}
